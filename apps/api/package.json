{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python build: no-op'",
    "lint": "uv run ruff check ."
  }
}
